# Cloudflare / Wrangler — only the local build cache + secrets file
# stay out of the repo. The deploy config (wrangler.jsonc, _headers,
# _redirects, .assetsignore) IS in the repo so Workers Builds can
# auto-deploy on push.
.wrangler/
.dev.vars

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Local notes
notes.local.md
*.local.md
